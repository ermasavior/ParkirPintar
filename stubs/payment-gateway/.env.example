PORT=8088

# Public base URL used to build QR code URLs returned to the Payment Service
BASE_URL=http://localhost:8088

# Must match WEBHOOK_SECRET in the Payment Service
WEBHOOK_SECRET=<your-webhook-secret>

# Webhook endpoint on the Payment Service that receives callback POSTs
PAYMENT_CALLBACK_URL=http://localhost:8087/webhook/payment/callback
